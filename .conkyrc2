conky.config = {
    maximum_width=420,
    gap_x=0,
    gap_y=40,

    alignment='top_right',
    background=false,
    border_inner_margin=0,
    border_outer_margin=0,
    border_width=0,
    cpu_avg_samples=2,
    default_color='efefef',
    double_buffer=true,
    draw_borders=true,
    draw_graph_borders=true,
    draw_outline=false,
    draw_shades=false,
    imlib_cache_size=10,
    net_avg_samples=2,
    no_buffers=true,
    override_utf8_locale=true,
    own_window=true,
    own_window_class='override',
    own_window_argb_visual=true,
    own_window_hints ='undecorated,below,sticky,skip_taskbar,skip_pager,above',
    own_window_title='equalizer=widget',
    own_window_transparent=true,
    own_window_type='desktop',
    pad_percents=2,
    short_units=true,
    top_name_width=15,
    update_interval=1.0,
    uppercase=false,
    use_xft=true,
    font='DejaVu Sans Mono:size=8',
}

conky.text = [[
-- One liners

- Paste on insert mode: <C-r>0
- Select on visual and run :sort
- Highlight column: set cc: 48

<Leader>f   Format
<Leader>w'  VimWiki Horizontal
<Leader>w\  VimWiki Vertical
<Leaver>s   Sort selected
<Leader>tt  Terminal

--

h j k l move one character
⬅ ⬇ ⬆ ➡

<C-B>   <C-F>     Page up/doww
<C-U>   <C-D>     Half page up/down
<C-p>   <C-n>     Autocomplete Visual/Insert

b   w   Previous/next word
%   Jump to matching bracket { } [ ] ( )

H   M   L   Move to top/middle/bottom of screen

z   Scroll the line with the cursor to:
  zt  Top of the screen
  zz  Center of the screen
  zb  Bottom of the screen

m{a-z}  Mark current position as {a-z}
`{a-z}  Move to position {a-z}
``      Move to last position

n   N   Next/previous matching search pattern
*   $   Whole word under cursor

-- Case

~    Toggle case
g{U/u}   Uppercase/Lowercase
g{UU/uu}  Uppercase current line

-- Edit

c   C   Change / Until end of line
a   A   Append / From the end of line
s   S   Delete char/line and insert
o   O   Next/Previous Line
r   R   Replace one char, Enter replace mode
u   <C-R> Undo/Redo change

cc  Replace current line
ciw Replace word

-- Visual mode

<C-V>    Enter visual block mode
v   Select visual model
d   Delete selection
V   Enter visual line mode
s   Replace selection
y   Copy selection

-- Commands

:ab h Hello World   h expands to "Hello world"
:ab                 List abbreviations
:una h              Remove abbreviation
:abc                Clear all abbreviations
h<C+V>              Avoid expansion

]]
