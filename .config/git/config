[user]
email = mariocesar.c50@gmail.com
name = Mario César

[core]
editor = nvim
autocrlf = input
quotepath = false
excludesfile = /Users/mariocesar/.git-excludesfile

[alias]
ci = commit
co = checkout
st = status
remotes = remote --verbose
tags = tag -l
branches = branch --all
aliases = config --get-regexp alias
search = "!f() { git grep \"$1\" $(git rev-list --all); } && f"

ls = log --pretty=oneline -n 20 --graph --abbrev-commit
s = status -s

[color]
ui = true
branch = auto
diff = auto
status = auto
interactive = auto

[merge]
summary = true
ff = no
commit = no

[pull]
default = matching
rebase = true

[push]
default = current

[credential]
helper = store

[init]
defaultBranch = main

[branch]
autoSetupRebase = always

[filter "lfs"]
clean = git-lfs clean -- %f
smudge = git-lfs smudge -- %f
process = git-lfs filter-process
required = true
