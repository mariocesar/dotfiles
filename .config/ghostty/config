# ~/.config/ghostty/config
# macOS · tuned for long agent sessions (Claude Code, Codex, etc.)

# ── Font ─────────────────────────────────────────────────────────────
font-family = "Fira Code"
font-size = 8
font-thicken = true

# Ligatures are nice in an editor, confusing in agent diff output.
# Uncomment to disable:
font-feature = -calt
font-feature = -liga

# Slightly looser lines make long log/diff dumps easier to scan.
adjust-cell-height = 8%

# ── Window ───────────────────────────────────────────────────────────
window-decoration = true
gtk-titlebar = false
macos-titlebar-style = transparent
macos-non-native-fullscreen = false
window-padding-x = 12
window-padding-y = 12
window-padding-balance = true
window-padding-color = extend
window-save-state = always
resize-overlay = never

background-opacity = 0.95
background-blur = true
minimum-contrast = 1.1
alpha-blending = linear-corrected

# ── macOS keyboard ───────────────────────────────────────────────────
# Without this, Option produces ø/å instead of Alt — breaks alt+f,
# alt+b, alt+. and most shell/TUI word-motion bindings.
macos-option-as-alt = left

# ── Cursor ───────────────────────────────────────────────────────────
cursor-style = block
cursor-style-blink = true

# ── Scrollback ───────────────────────────────────────────────────────
# NOTE: this is BYTES, not lines. Default is 10 MB. 100 MB holds a
# full day of agent output; bump down if you keep 15 splits open.
scrollback-limit = 100000000

# ── Behaviour ────────────────────────────────────────────────────────
mouse-hide-while-typing = true
copy-on-select = false
# `always`, not `true`: cmd+w should confirm on an idle tab too.
confirm-close-surface = always
app-notifications = no-clipboard-copy,no-config-reload

# Splits
unfocused-split-opacity = 0.7
unfocused-split-fill = #44464f

# ── Shell integration ────────────────────────────────────────────────
shell-integration = detect
shell-integration-features = cursor,sudo,title

# ── Keybindings ──────────────────────────────────────────────────────
# macOS defaults already cover cmd+t / cmd+n / cmd+d / cmd+shift+d /
# cmd+plus / cmd+minus / cmd+0. Don't shadow ctrl+* — TUIs need it.
# On Linux, `cmd+` maps to `super+`, which niri (the WM) claims heavily —
# an app-level super+ bind never reaches Ghostty if niri already owns
# that chord. Prefer ctrl+alt/ctrl+shift combos for anything Linux needs.

# Multiline input in Claude Code and friends.
keybind = shift+enter=text:\n

keybind = ctrl+shift+k=clear_screen

# Default resize_split chord (super+ctrl+shift+arrow) collides with
# niri's Mod+Ctrl+Shift+arrow (move-column-to-monitor-*) — niri eats it
# before Ghostty sees it. Add a niri-safe alternate that still resizes.
keybind = ctrl+alt+shift+arrow_up=resize_split:up,10
keybind = ctrl+alt+shift+arrow_down=resize_split:down,10
keybind = ctrl+alt+shift+arrow_left=resize_split:left,10
keybind = ctrl+alt+shift+arrow_right=resize_split:right,10

# ── Theme ────────────────────────────────────────────────────────────
# `ghostty +list-themes` to browse a built-in theme instead.
config-file = ./config-dankcolors
