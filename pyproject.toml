[project]
name = "dotfiles"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mario-César", email = "mariocesar.c50@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
dotfiles = "dotfiles:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
